作成ファイル:

　run_v049_gabor_matlab_compat_grid.sh

特徴:

　実行対象は columnar_ed_ann_v049_gabor_experiment.py
　--matlab_compat を自動で付与して実行
　9条件（推奨Gabor候補）を逐次実行
　4層/5層を選択可能（--mode h4|h5|both）
　ログは既存ルールに沿った名前で v049 に保存
　並行実行なし（完全逐次）

デフォルト実行条件:

　dataset=fashion
　train=10000, test=10000, epochs=10
　lr=0.03, gc=0.03
　H4/H5のネットワーク側パラメータは固定（Gabor条件のみ比較）

使い方:

　4層+5層両方:
    bash scripts/run_v049_gabor_matlab_compat_grid.sh

　4層のみ:
    bash run_v049_gabor_matlab_compat_grid.sh --mode h4

　5層のみ:
    bash run_v049_gabor_matlab_compat_grid.sh --mode h5

　データ数やepoch変更:
    bash run_v049_gabor_matlab_compat_grid.sh --mode h4 --train 5000 --test 5000 --epochs 5

補足:

構文チェック済み（bash -n）。
実行はしていないので、別PCへこの2ファイルをコピーすれば開始できます。
columnar_ed_ann_v049_gabor_experiment.py
run_v049_gabor_matlab_compat_grid.sh
