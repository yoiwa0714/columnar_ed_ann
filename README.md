**日本語** | [English](README_en.md)

# **【 コラムED法 】 フルバージョン - オリジナルED法にコラム構造を導入した拡張**

[![Python](https://img.shields.io/badge/Python-3.9%2B-blue.svg)](https://www.python.org/)
[![NumPy](https://img.shields.io/badge/NumPy-1.19%2B-orange.svg)](https://numpy.org/)

## 目次

- [概要](#概要)
- [特徴](#特徴)
- [クイックスタート](#クイックスタート)
- [使用例](#使用例)
- [コマンドライン引数](#コマンドライン引数)
- [動作原理](#動作原理)
  - [オリジナルED法とは](#オリジナルed法とは)
  - [コラム構造](#コラム構造)
  - [アミン拡散による重み更新](#アミン拡散による重み更新)
  - [Gabor特徴抽出](#gabor特徴抽出)
  - [リザバーコンピューティング的特性](#リザバーコンピューティング的特性)
- [達成精度](#達成精度)
- [ディレクトリ構成](#ディレクトリ構成)
- [パラメータ自動設定](#パラメータ自動設定)
- [オリジナルED法への準拠](#オリジナルed法への準拠)
- [参考文献](#参考文献)
- [ライセンス](#ライセンス)
- [謝辞](#謝辞)

---

## 概要

**コラムED法**は、金子勇氏が考案した誤差拡散学習法（ED法、以下オリジナルED法）に、大脳皮質のコラム構造を導入することによってオリジナルED法を拡張したニューラルネットワーク実装です。

コラムED法では**微分の連鎖律を用いた誤差逆伝播法（Backpropagation）を一切使用せず**、生物学的に妥当なアミン拡散機構によって学習を行っていますが、MNIST手書き数字認識で **97.01%** のテスト精度を達成しています（3層構成、訓練10,000サンプル、30エポック）。

本リポジトリでは以下の2つの実装を提供しています：

| 実装 | ファイル | 用途 |
|------|---------|------|
| **フルバージョン** | `columnar_ed_ann.py` | 全パラメータを指定可能。本ドキュメントの対象 |
| 簡易版 | `columnar_ed_ann_simple.py` | 最小限の引数で高精度を実現。詳細は [README_simple.md](README_simple.md) 参照 |


> **簡易版との主な違い:**
> - Gabor特徴抽出はデフォルトOFF `--gabor_features` で明示的に有効化（簡易版はデフォルトON）
> - 学習率・アミン拡散係数・動的シナプス刈り込み・勾配クリッピング・早期停止等をコマンドラインから指定可能。パラメータの探索や詳細な実験が可能
> - GPU(CuPy)対応
> - Gaborフィルタの詳細パラメータ（方位数・周波数・カーネルサイズ等）を指定可能

---

## 特徴

### 1. 微分の連鎖律を用いた誤差逆伝播法を使用しない

従来のニューラルネットワークでは「微分の連鎖律を用いた誤差逆伝播法」で重みを更新しますが、本実装ではこれを一切使用しません。代わりに、脳の神経伝達物質（アミン）の拡散をモデル化した機構で学習を行います。

### 2. 大脳皮質のコラム構造

大脳皮質の視覚野に見られるコラム構造を導入し、一部のニューロンを特定のクラスに割り当てます。これにより、オリジナルED法が苦手としていた1つの重み空間での多クラス分類に対応しています。

### 3. Gabor特徴抽出

大脳皮質V1野の単純型細胞をモデル化したGaborフィルタによる特徴抽出を搭載しています（`--gabor_features` で有効化）。入力品質の向上により、適切なパラメータ設定であれば1層構成でも95%超の精度を達成します。

### 4. 生物学的に妥当な機能のみで学習

誤差関数の最小化や微分の連鎖律などの数学的な最適化理論に依存せず、生物学的に妥当な機能（アミン拡散、コラム構造、Gaborフィルタ）のみで学習を行います。

### 5. 学習が速い

学習データ数が十分に大きい場合には、1エポック目で最終テスト精度の90%以上の精度に到達します。多くのエポックを繰り返す必要がなく、少ないエポック数で高い精度が得られます。

### 6. パラメータ調整が容易

パラメータ変更に対して安定的・単調的に反応し、誤差逆伝播法で見られるような勾配消失・勾配爆発による突然の学習崩壊が発生しません。パラメータが最適値から多少外れても精度が大きく下落するようなことは生じ難いため、ハイパーパラメータのチューニングが容易です。

### 7. リザバーコンピューティング的特性

各コラムに属するニューロン数を1個にした場合（`--column_neurons 1`を指定した場合）には、リザバーコンピューティング方式での動作となります。隠れ層のコラムに属さないニューロンを固定ランダム重み（リザバー）として保持し、少数のコラムニューロンのみをオリジナルED法で学習させます。

### 8. 動的シナプス刈り込み

脳の発達的プルーニングをモデル化した動的シナプス刈り込み機能を搭載しています（`--dynamic_pruning_fs` で有効化）。学習中に不要なシナプス結合を段階的に除去することで、ネットワークの計算効率を向上させます。

---

## クイックスタート

### 1. インストール

```bash
# リポジトリのクローン
git clone https://github.com/yoiwa0714/columnar_ed_ann.git
cd columnar_ed_ann

# 仮想環境の作成（推奨）
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows

# 依存パッケージのインストール
pip install -r requirements.txt
```

### 2. 実行

```bash
# 1層 + Gabor特徴（約3分）
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features

# 1層 + Gabor特徴 + 可視化（学習曲線・混同行列・活性化ヒートマップ）
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --viz --heatmap
```

seed=42（デフォルト）で約96%のテスト精度が得られます。

### 3. 結果確認

実行が完了すると、以下のような結果が表示されます：

```
======================================================================
学習完了
======================================================================
最終精度: Train=0.9722, Test=0.9613
ベスト精度: Test=0.9613 (Epoch 10)
```

---

## 使用例

### 基本的な実行パターン

```bash
# 1層 + Gabor特徴（約3分）
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features
# → Test ≈ 96.13%

# 2層 + Gabor特徴（約10分）
python columnar_ed_ann.py --hidden 2048,1024 --train 10000 --test 10000 --gabor_features
# → Test ≈ 96.85%

# 3層 + Gabor特徴（約20分、最高精度）
python columnar_ed_ann.py --hidden 2048,1024,1024 --train 10000 --test 10000 --gabor_features
# → Test ≈ 97.01%

# Gabor無し（オリジナルED法の純粋な学習力を確認）
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000
# → Test ≈ 90.37%
```

### 可視化

```bash
# リアルタイム学習曲線を表示
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --viz

# 学習曲線 + 隠れ層・出力層ヒートマップ
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --viz --heatmap

# 可視化結果をディレクトリに保存（タイムスタンプ付き自動命名）
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --viz --heatmap --save_viz results/

# ファイル名を指定して保存
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --viz --heatmap --save_viz results/my_experiment.png

# 不正解学習データの一覧表示（最終エポック完了後にスクロール可能ウィンドウで表示）
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --show_train_errors

# クラスごとの表示上限数を指定（デフォルト: 100）
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --show_train_errors --max_errors_per_class 50
```

### 学習パラメータの指定

```bash
# 学習率を手動指定
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features \
    --output_lr 0.15 --column_lr 0.0015

# アミン拡散係数を調整
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features \
    --u1 0.5 --u2 0.8

# 2層で勾配クリッピングを指定
python columnar_ed_ann.py --hidden 2048,1024 --train 10000 --test 10000 --gabor_features \
    --gradient_clip 0.03
```

### 動的シナプス刈り込み

```bash
# 40%の重みを刈り込み（脳の発達的プルーニングに相当）
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features \
    --dynamic_pruning_fs 0.4

# 刈り込みの詳細ログを表示
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features \
    --dynamic_pruning_fs 0.4 --pruning_verbose

# 刈り込み開始・終了エポックを指定
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --epochs 20 --gabor_features \
    --dynamic_pruning_fs 0.4 --pruning_start_epoch 3 --pruning_end_epoch 15
```

### 早期停止

```bash
# グリッドサーチ時の早期停止（Epoch 3でTest精度15%以下なら停止）
python columnar_ed_ann.py --hidden 2048 --train 10000 --test 10000 --gabor_features \
    --early_stop_epoch 3 --early_stop_threshold 0.15
```

### その他のオプション

```bash
# Fashion-MNIST
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --dataset fashion

# コラムニューロン数・初期化スケールを手動指定（YAMLのデフォルトを上書き）
python columnar_ed_ann.py --hidden 2048,1024 --gabor_features --column_neurons 10 --init_scales 0.7,1.8,0.8

# 重み初期化手法を変更
python columnar_ed_ann.py --hidden 2048 --train 5000 --test 5000 --gabor_features --init_method xavier

# YAML設定一覧を表示
python columnar_ed_ann.py --list_hyperparams

# コラム構造の診断
python columnar_ed_ann.py --hidden 2048 --diagnose_column
```

---

## コマンドライン引数

### ネットワーク構成

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--hidden` | `2048` | 隠れ層ニューロン数（例: `2048`=1層, `2048,1024`=2層） |
| `--train` | `3000` | 訓練サンプル数 |
| `--test` | `1000` | テストサンプル数 |
| `--epochs` | YAML自動 | エポック数 |
| `--seed` | `42` | 乱数シード |
| `--dataset` | `mnist` | データセット名（`mnist`, `fashion`, `cifar10`）またはカスタムデータパス（詳細は [CUSTOM_DATASET_GUIDE.md](CUSTOM_DATASET_GUIDE.md)） |
| `--batch_size` | なし | ミニバッチサイズ（未指定時はオンライン学習） |
| `--use_cupy` | OFF | CuPy(GPU)高速化を有効化 |

### 学習パラメータ

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--output_lr` | YAML自動 | 出力層学習率 |
| `--non_column_lr` | YAML自動 | 非コラムニューロン層別学習率（カンマ区切り） |
| `--column_lr` | YAML自動 | コラムニューロン層別学習率（カンマ区切り） |
| `--u1` | YAML自動 | アミン拡散係数 u1 |
| `--u2` | YAML自動 | アミン拡散係数 u2 |
| `--gradient_clip` | `0.03` | 勾配クリッピング値 |

### コラム構造

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--column_neurons` | YAML自動 | 各クラスのコラムニューロン数 |
| `--base_column_radius` | `0.4` | 基準コラム半径 |
| `--column_radius` | 自動計算 | コラム影響半径 |
| `--participation_rate` | `0.1` | コラム参加率 |
| `--diagnose_column` | OFF | コラム構造の詳細診断を実行 |
| `--diagnose_hidden_weights` | OFF | 隠れ層の重み状態を詳細診断 |

### 初期化

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--init_method` | `he` | 重み初期化手法（`uniform`, `xavier`, `he`） |
| `--hidden_sparsity` | YAML自動 | 隠れ層スパース率（層別カンマ区切り対応） |
| `--init_scales` | YAML自動 | 層別初期化スケール（例: `0.7,1.8,0.8`） |

### 可視化

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--viz` | OFF | リアルタイム学習曲線を表示 |
| `--heatmap` | OFF | ヒートマップ表示（`--viz`と併用） |
| `--save_viz` | なし | 可視化結果の保存先ディレクトリ |
| `--save_weights` | OFF | エポックごとの重み統計を保存 |
| `--show_train_errors` | OFF | 最終エポックの不正解学習データを一覧表示 |
| `--max_errors_per_class` | `100` | クラスごとの表示上限数 |

### 早期停止

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--early_stop_epoch` | なし | 早期停止判定を行うエポック（未指定で無効） |
| `--early_stop_threshold` | `0.15` | Test精度がこの値以下なら停止 |

### 動的シナプス刈り込み

脳の発達的プルーニングをモデル化。学習中に不要なシナプス結合を段階的に除去します。

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--dynamic_pruning_fs` | なし | 目標スパース率（例: `0.4`=40%刈り込み、指定で有効化） |
| `--pruning_start_epoch` | 自動検出 | 刈り込み開始エポック |
| `--pruning_end_epoch` | 最終エポック | 刈り込み終了エポック |
| `--stability_threshold` | `0.01` | 安定期検出の閾値（Test精度変化率） |
| `--pruning_verbose` | OFF | 刈り込み詳細ログを出力 |

### Gabor特徴抽出

V1単純型細胞を模した固定フィルタで入力特徴を抽出します。

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--gabor_features` | OFF | Gabor特徴抽出を有効化 |
| `--gabor_orientations` | `8` | フィルタ方位数 |
| `--gabor_frequencies` | `2` | 空間周波数の数 |
| `--gabor_kernel_size` | `7` | フィルタカーネルサイズ |
| `--gabor_pool_size` | `4` | 平均プーリングウィンドウサイズ |
| `--gabor_pool_stride` | `4` | プーリングストライド |
| `--gabor_no_edge` | OFF | Sobelエッジフィルタを含めない |

### ユーティリティ

| 引数 | デフォルト | 説明 |
|------|-----------|------|
| `--list_hyperparams` | - | YAML設定一覧を表示（層数指定可: `--list_hyperparams 2`） |

---

## 動作原理

> 📖 コード内部の動作フローとコアアルゴリズムの詳細解説は **[コラムED法 動作の流れ](docs/ja/コラムED法_動作の流れ.md)** を参照してください。

### コラムED法とは

**コラムED法（Columnar Error Diffusion Method）** は、金子勇氏が考案したオリジナルED法のニューラルネットワークに、大脳皮質のコラム構造を導入した拡張実装です。

オリジナルED法は、脳の神経伝達物質の拡散をモデル化した生物学的に妥当な学習アルゴリズムですが、2クラス分類を前提として設計されていたため、10クラス以上の多クラス分類を行うことが困難でした。その原因は、ネットワーク内の隠れ層ニューロンがどの出力クラスに寄与するのかという情報を持っていなかったことにあります。

コラムED法では、大脳皮質に見られるコラム構造を導入し、隠れ層の**コラムニューロン**と**出力クラスニューロン**の対応関係を明確にしました。これにより、各コラムニューロンが特定のクラスの学習信号のみを受け取る仕組みが実現し、1つのネットワーク（重み空間）で多クラス分類が可能になりました。

さらに、大脳皮質V1野の単純型細胞をモデル化したGaborフィルタによる特徴抽出を組み合わせることで、学習メカニズム自体を変更することなく、テスト正解率が約6%向上しています（MNIST 1層: 90.37% → 96.13%）。

### オリジナルED法とは

**誤差拡散学習法（Error Diffusion Learning Algorithm: オリジナルED法）** は、金子勇氏が1999年に考案したニューラルネットワークの学習アルゴリズムです。

現在のニューラルネットワーク学習方法の主流である「微分の連鎖律を用いた誤差逆伝播法（BP法）」は、ネットワークの出力と正解データとの誤差を計算し、その結果を微分の連鎖律を用いて出力層から入力層に向かって逆方向に伝播させることで、各層のニューロンの重みとバイアスを効率的に更新するというものです。

しかし、オリジナルED法を考案された金子勇氏は、この生物学的に不自然な学習方法を受け入れることができず、生物学的に妥当な脳の神経伝達物質（ノルアドレナリン、ドーパミン等のアミン系物質）が空間に拡散する現象をモデル化し、オリジナルED法を開発されました。オリジナルED法では、アミン系物質の拡散情報に基づいて各層が**独立して**重みを更新します。

**BP法との本質的な違い:**

| | BP法（誤差逆伝播法） | オリジナルED法（誤差拡散学習法） |
|---|---|---|
| 誤差の伝播 | 微分の連鎖律で軸索を逆流 | アミン濃度として空間に拡散 |
| 各層の学習 | 後段の勾配に依存 | **各層が独立して学習** |
| 生物学的妥当性 | 低い | 高い |

> 詳細は [docs/ja/ED法_解説資料.md](docs/ja/ED法_解説資料.md) を参照してください。

### アミン拡散による重み更新

オリジナルED法の学習は、以下の手順で行われます：

```
1. 順伝播: 入力 → 隠れ層(tanh) → 出力層(SoftMax) → 予測
2. 誤差計算: 正解クラスの確率誤差 → アミン濃度に変換
3. アミン拡散: 出力層 → 隠れ層（コラム構造に沿って選択的に拡散）
4. 重み更新: アミン濃度 × 飽和抑制項 × 入力 で各層が独立に更新
```

**飽和抑制項**（オリジナルED法の核心）:

```
飽和抑制項 = |z| × (1 - |z|)
```

- これはシグモイド微分 `z(1-z)` でも tanh微分 `1-z²` でもありません
- ニューロンの活性値 `z` が飽和（±1に近い）するほど更新量が小さくなる仕組みです
- 微分の連鎖律を使わずに、各層が独立して適切な更新量を決定できます

### コラム構造

大脳皮質の第一次視覚野や連合野には、似た特性を持つニューロンが柱（コラム）状に集まった構造が見られます[*1]。本実装では、この構造を取り入れることでオリジナルED法の多クラス分類能力を拡張しています。

**コラム構造の動作:**

1. 隠れ層のニューロンをハニカム構造（2-3-3-2配置）で2次元空間に配置
2. 各クラスのコラム中心を空間上に配置（10クラスなら10個）
3. コラム中心に最も近いニューロンがそのクラスの**コラムニューロン**として割り当てられる
4. 学習時、正解クラスのコラムニューロンのみがアミン信号を受け取って重みを更新
5. コラムに属さないニューロンは固定ランダム重みのまま保持される（`column_neurons`の設定により学習ニューロン(コラムに属するニューロン)数は変わる）

![ハニカム構造の空間配置](images/hexagonal_column_structure_no_origin.png)

*図: ハニカム構造におけるニューロン配置（2048ニューロン、column_neurons=1）。グレーの点が非コラムニューロン（2038個）、色付きの点がコラムニューロン（10個）、★がクラス中心（0-9）*

```
[*1] 「NeUro+(ニューロプラス)」東北大学の知見✕日立の技術による脳科学ベンチャー
     (https://neu-brains.co.jp/neuro-plus/glossary/ka/140/)
```

### Gabor特徴抽出

大脳皮質V1野（第一次視覚野）の単純型細胞は、特定の方位と周波数のエッジに選択的に反応します。本実装では、これをGaborフィルタでモデル化し、入力画像の特徴を抽出します。

**フィルタ構成:**
- 8方位 × 2周波数 = 16 Gaborフィルタ + 2 Sobelエッジフィルタ = **18フィルタ**
- カーネルサイズ: 7×7、プーリング: 4×4平均プーリング
- 出力次元: 784 → 882（MNIST 28×28の場合）

**効果:** Gabor特徴により、1層構成でもMNIST 90.37% → 96.13%（+5.76%）に向上します。学習メカニズム自体は変更せず、入力品質の向上によって精度を改善する生物学的に妥当なアプローチです。

> フルバージョンでは `--gabor_orientations`, `--gabor_frequencies`, `--gabor_kernel_size`, `--gabor_pool_size`, `--gabor_pool_stride`, `--gabor_no_edge` でフィルタの詳細パラメータを調整できます。

### リザバーコンピューティング的特性

`column_neurons=1`（デフォルト: 1層構成）の設定では、本実装はリザバーコンピューティングと同等の動作原理で機能します。

**なぜcn=1でリザバーコンピューティングになるのか:**

`column_neurons=1`は各クラスに1個のコラムニューロンを割り当てる設定です。2048ニューロンの隠れ層で10クラス分類を行う場合、学習対象になるのはわずか10個（全体の0.5%）で、残り2038個のニューロンは初期化時のランダム重みが固定されたまま変化しません。

この構成は、リザバーコンピューティングの基本原理と正確に一致します：

| リザバーコンピューティング | コラムED法（cn=1） |
|---|---|
| **リザバー**: 固定ランダム結合で入力を高次元空間に射影 | **非コラムニューロン（2038個）**: 固定ランダム重みで入力を高次元空間に射影 |
| **読み出し層**: リザバーの出力から分類境界を学習 | **出力層**: 隠れ層全体の活性化パターンから分類を学習 |

固定ランダム重みのニューロン群は、入力データを高次元の非線形空間にランダム射影する役割を果たします。異なるランダム重みを持つ各ニューロンが入力の異なる側面に反応するため、元の入力空間では線形分離が困難なパターンでも、高次元射影後には分離しやすくなります。出力層はこの射影された表現から分類境界を学習するだけでよいため、少数のニューロンの学習だけで高い分類精度を達成できます。

この構成により、コラムED法は生物学的妥当性（少数のニューロンのみが学習）と高い汎化性能（固定重みによる過学習抑制）を両立しています。

**cn>1の場合:**

例えば`column_neurons=10`（デフォルト: 2層以上の構成）では、各クラスに10個のコラムニューロンが割り当てられます。2048ニューロンの隠れ層では100個（全体の約4.9%）が学習対象となり、残り1948個が固定ランダム重みを保持します。

cn=1と比較すると、学習に参加するニューロンが増えることで各クラスをより多様な特徴で表現できるようになります。リザバーコンピューティング的な構造（大部分が固定重み）は維持されたまま、コラムニューロンの増加により分類性能が向上します。2層以上の構成ではcn=10がデフォルトとなり、3層構成で97.01%の精度を達成しています。

---

## 達成精度

MNIST手書き数字認識での実験結果（seed=42、再現可能）:

### Gabor特徴あり（`--gabor_features`）

| 構成 | 隠れ層 | テスト精度 | 実行時間 (※) |
|------|--------|-----------|----------------|
| 1層 | [2048] | 96.13% | 約3分 |
| 2層 | [2048, 1024] | 96.85% | 約10分 |
| 3層 | [2048, 1024, 1024] | **97.01%** | 約20分 |

※ 実行時間は第11世代 Core i5 / RTX 3060 環境での測定値であり、実行環境によって異なります。

### Gabor特徴なし

| 構成 | 隠れ層 | テスト精度 |
|------|--------|-----------|
| 1層 | [2048] | 90.37% |
| 2層 | [2048, 1024] | 89.38% |
| 3層 | [2048, 1024, 1024] | 89.41% |

> **実験条件:** 訓練10,000サンプル、テスト10,000サンプル、seed=42（すべて同一条件で再現可能）。エポック数は層構成により異なります（1層: 10、2層: 20、3層: 30 — `config/hyperparameters.yaml` で自動設定）。

> **注:** すべての層構成で、`config/hyperparameters.yaml` からコラムニューロン数や初期化スケールが自動的に最適値に設定されます（6層以上は5層のパラメータをフォールバックとして使用）。訓練データ量やエポック数を増やすことで、さらに高い精度が得られます（例: 2層+Gabor, 20kサンプルで97.43%）。

---

## ディレクトリ構成

```
columnar_ed_ann/
├── columnar_ed_ann.py              # ★ フルバージョンメインスクリプト（本ドキュメント対象）
├── columnar_ed_ann_simple.py       # 簡易版メインスクリプト
├── README.md                       # ★ 本ドキュメント（フルバージョン用・日本語）
├── README_en.md                    # 本ドキュメント（フルバージョン用・英語）
├── README_simple.md                # 簡易版ドキュメント（日本語）
├── README_simple_en.md             # 簡易版ドキュメント（英語）
├── LICENSE                         # ライセンス
├── requirements.txt                # 依存パッケージ
├── CUSTOM_DATASET_GUIDE.md         # カスタムデータセット利用ガイド
│
├── modules/                        # ★ フルバージョン用モジュール
│   ├── ed_network.py               #   EDネットワーク本体（学習・評価）
│   ├── column_structure.py         #   コラム構造生成（ハニカム配置）
│   ├── gabor_features.py           #   Gabor特徴抽出（V1単純型細胞モデル）
│   ├── activation_functions.py     #   活性化関数（tanh、softmax）
│   ├── neuron_structure.py         #   E/Iペア構造（Dale's Principle）
│   ├── data_augmentation.py        #   データ拡張（`--augment` オプション使用時に参照）
│   ├── hyperparameters.py          #   YAMLパラメータ読み込み
│   ├── data_loader.py              #   データセット読み込み
│   └── visualization_manager.py    #   可視化（学習曲線、ヒートマップ、不正解学習データ一覧）
│
├── modules_simple/                 # 簡易版モジュール
├── config/                         # パラメータ設定ファイル
│   ├── hyperparameters.yaml        #   層数別最適パラメータ（編集可能）
│   └── hyperparameters_initial.yaml#   初期状態（復元用）
├── docs/                           # 関連ドキュメント
│   ├── ja/ED法_解説資料.md          #   オリジナルED法の詳細解説（日本語）
│   ├── ja/EDLA_金子勇氏.md          #   ED法の学術的背景と金子勇氏の業績（日本語）
│   ├── ja/コラムED法_動作の流れ.md  #   コラムED法のコア関数詳細解説（日本語）
│   ├── en/ED_Method_Explanation.md  #   オリジナルED法の詳細解説（英語）
│   ├── en/EDLA_Isamu_Kaneko.md      #   ED法の学術的背景と金子勇氏の業績（英語）
│   └── en/Columnar_ED_Method_Flow.md#   コラムED法のコア関数詳細解説（英語）
├── images/                         # コラム構造図
└── original-c-source-code/         # 金子勇氏オリジナルCソースコード
```

---

## パラメータ自動設定

フルバージョンでも、隠れ層数に応じた最適パラメータが `config/hyperparameters.yaml` から自動的に読み込まれます。コマンドライン引数で明示的に指定した値は自動設定より優先されます。

### 自動設定される主なパラメータ

| パラメータ | 1層 | 2層 | 3層 | 説明 |
|-----------|------|------|------|------|
| output_lr | 0.15 | 0.15 | 0.20 | 出力層学習率 |
| non_column_lr | [0.15] | [0.15, 0.15] | [0.20, 0.20, 0.20] | 隠れ層基準学習率（層別）※1 |
| column_lr | [0.0015] | [0.00075, 0.00045] | [0.001, 0.0006, 0.0004] | コラムニューロン学習率（層別） |
| column_neurons | 1 | 10 | 10 | コラムニューロン数 |
| init_scales | [0.4, 1.0] | [0.7, 1.8, 0.8] | [0.7, 1.8, 1.8, 0.8] | 層別初期化スケール |
| hidden_sparsity | 0.4 | [0.4, 0.4] | [0.4, 0.4, 0.4] | 隠れ層スパース率 |
| gradient_clip | 0.05 | 0.03 | 0.03 | 勾配クリッピング |

> **3系統学習率**: `output_lr`（出力層）、`non_column_lr`（隠れ層基準、層別）、`column_lr`（コラムニューロン、層別）の3系統で学習率を独立制御します。
>
> ※1 `non_column_lr` は隠れ層全体の基準学習率として使用されます。非コラムニューロンはアミン信号を受け取らないため実際には学習せず、学習するのはコラムニューロン（`column_lr`の学習率で更新）と出力層（`output_lr`の学習率で更新）のみです。

### カスタマイズ方法

1. **コマンドライン引数**: `--output_lr`, `--column_lr`, `--column_neurons`, `--init_scales` 等で上書き可能
2. **YAML直接編集**: `config/hyperparameters.yaml` をエディタで編集
3. **設定確認**: `python columnar_ed_ann.py --list_hyperparams` で一覧表示

> 万が一YAMLファイルを壊した場合は、`config/hyperparameters_initial.yaml` からコピーして復元できます。

---

## オリジナルED法への準拠

金子勇氏のCソースコードをリファレンスとし、オリジナルED法に準拠した実装であることを確認しています。

| 項目 | 実装 | 準拠 |
|------|------|------|
| 出力層飽和項 | `\|z\| × (1 - \|z\|)` | ✓ |
| 隠れ層飽和項 | `\|z\| × (1 - \|z\|)` | ✓ |
| アミン拡散 | コラム構造に沿った選択的拡散 | ✓ |
| 重み更新 | アミン濃度ベース、微分の連鎖律不使用 | ✓ |
| Dale's Principle | 興奮性・抑制性ニューロンペア | ✓ |
| SoftMax | 出力の確率化のみに使用（順伝播） | ✓ |

---

## 参考文献

- [オリジナルED法 解説資料（日本語）](docs/ja/ED法_解説資料.md) — オリジナルED法の理論と動作の詳細解説
- [EDLA — 金子勇氏のError Diffusion学習法（日本語）](docs/ja/EDLA_金子勇氏.md) — ED法の学術的背景と金子勇氏の業績
- [金子勇氏 (1999) オリジナルED法 Cソースコード](original-c-source-code/main.c) — 本実装の基となったオリジナル実装
- [大脳皮質のコラム構造について](https://neu-brains.co.jp/neuro-plus/glossary/ka/140/) — コラム構造の生物学的背景

---

## ライセンス

[LICENSE](LICENSE) ファイルを参照してください。

- **非商用利用**: MITライセンス（個人利用・学術研究・教育目的）
- **商用利用**: 別途商用ライセンスが必要（GitHubのIssuesで連絡）

---

## 謝辞

本実装は、金子勇氏が1999年に考案されたError-Diffusion（ED）学習法に基づいています。生物学的に妥当なニューラルネットワーク学習アルゴリズムにおける氏の先駆的な業績に対し、深い敬意と感謝の意を表します。

---

## 著者

yoiwa0714

---

**注意**: 本実装は研究・教育目的です。商用利用する場合は、[LICENSE](LICENSE) を確認してください。
